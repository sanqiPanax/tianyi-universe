# 天意宇宙 · 素材快剪方案

## 素材情况
- 95 集 MKV 齐,`F:\ownWork\天意宇宙\新三国\新三国01.mkv` ~ `95.mkv`
- 单集约 40 分 50 秒(XTM HALFCD 压制,696×424,视频 h264 + 音频 HE-AAC 国语)
- 无内嵌字幕轨(硬字幕烧画面)
- ffmpeg 8.1 可用: `F:\school_work\AIOT\dayao\ffmpeg-8.1-essentials_build\bin\ffmpeg.exe`
  (注意:在 DSH 沙箱里必须用 node 子进程 + stdio inherit 方式调用,直接 pwsh 调会被挡)

## 产出目标
- 每个梗节点一个素材文件 → `天意宇宙\clips\{梗id}.mp3`(纯语音,小体积,网页优先用)
  - 可选 mp4 视频片段(名场面),网页会自动探测 mp3 优先
- 配套 `剪辑清单.md` 记录每个梗的出处(集数/时间戳),方便复核

## 工作流: 三段式(人工定位 → 批量粗剪 → 批量精修/命名)

### 阶段 A: 建立"梗 → 时间戳"清单(最费人工,决定质量)
给每个梗定位 (集, 起始秒, 结束秒)。方法:
1. 用播放器拖到大致集数 → 看硬字幕找台词 → 记时间。
2. 为减少来回:每集打开时把该集涉及的所有梗一次找齐(集内索引见下)。
3. 粗记区间即可(多剪前后 1~2 秒),精修由脚本做。

### 阶段 B: 批量粗剪(自动,快)
ffmpeg 按清单批量截取,每节点产出 mp3 + (可选)mp4:
```
ffmpeg -ss {start} -to {end} -i 新三国{ep:02d}.mkv -vn -acodec libmp3lame -q:a 5 clips/{id}.mp3
ffmpeg -ss {start} -to {end} -i 新三国{ep:02d}.mkv -c:v libx264 -crf 23 -preset fast -c:a aac -movflags +faststart clips/{id}.mp4
```
- 先裁宽范围(前后富余)→ 批量跑(95集不用全转,只转命中的集)

### 阶段 C: 听/看校验 + 修边界
- 抽查 mp3,修 start/end(改清单重跑单条)
- 网页 index.html 已支持 `clips/{id}.mp3` 自动播放,文件名与节点 id 对应即可

## 集数速查(按梗内容分布,方便"一集找齐")
(待补:按萌娘百科梗列表,把同集梗分组列出来,找的时候一次找全)

## 自动化辅助脚本规划
1. `tools/1_定位表.js` — 读取"梗清单(人工填时间)"CSV
2. `tools/2_批量剪辑.js` — 调 ffmpeg 批量剪(可断点续剪,跳过已有文件)
3. `tools/3_校验.js` — 统计已剪/未剪,输出缺失清单

## 关键经验/坑
- 沙箱:ffmpeg 只能 `node execFileSync(ff, args, {stdio:'inherit'})`,不能带 pipe(EPERM)
- 画质 696x424 足够做网页弹窗;若要 1080p 版需另下高清片源(不建议,成本高)
- HE-AAC 音频,ffmpeg 解出后转 mp3 无问题
